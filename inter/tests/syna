class Main
{
  static int a;
  static String ahojky;
  static int x;

  static void Abc(String a, String b)
  {

  }

  static int Run(int a, String b, double c)
  {
    prazdny_class.yolo();
    //int ahoj = (5 + 8 * 9 - 9 + (5 +4) + 5 * (8+5)*7 + 5) ;
    int ahoj = ahoj + 8 / 7;
    String str1;
    Main.x = 88 + ahoj;
    str1 = "Toto je nejaky text";
    String str2 = prazdny_class.yolo();
    str2 = str1 + ", ktery jeste trochu obohatime";
    int abc = Func(1);
    abc = ifj16.readInt();
    String asds;
    String str;
    ifj16.sort(str1);
    //ifj16.print("Pozice retezce \"text\" v retezci str2: " + x + "\n");
    ifj16.find(str, str);
    int cmp;
    a = cmp;
    while (cmp != 0)
    {

    }

    if(cmp)
    {
        a = 0;
    }

    if (0 >= 8 )
    {
        if (0)
        {
            ifj16.print("Zadejte cislo pro vypocet faktorialu: ");
        }
        else
        {
          a = ifj16.readInt();
          while (cmp != 0)
          {
              while (0)
              {
                a = ifj16.readInt();
                if (a + b) { return 10;}
                else { return; }
              }
          }
        }
    }
    else
    {
        return a + b +c / 55;
    }
  }
  static int Func(int a)
  {

  }
}

class prazdny_class
{
    static String yolo()
    {
    
    }

}
